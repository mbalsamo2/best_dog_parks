<h1 class="text-center"><%= "#{current_user.name}'s Dog Parks" %></h1>
<ol>
  <% @parks.each do |park| %>
    <li><%= park.name %></li>
  <% end %>
  <br><div><%= button_to "Add New Park", new_park_path , class: 'btn btn-info btn-md float-xl-none' %></div>
</ol>
